<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.cardview.widget.CardView
        app:cardCornerRadius="@dimen/dimen_small_margin"
        android:background="@color/colorWhite"
        android:layout_margin="@dimen/dimen_small_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <LinearLayout
            android:orientation="vertical"
            android:gravity="center_horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/ivIcon"
                android:layout_marginTop="@dimen/dimen_large_margin"
                app:srcCompat="@drawable/ic_poke_location"
                android:scaleType="centerInside"
                android:layout_width="wrap_content"
                android:layout_height="90dp"/>

            <TextView
                tools:text="@string/retrofit_timeout"
                android:id="@+id/tvMessage"
                android:textAlignment="center"
                android:layout_margin="@dimen/dimen_large_margin"
                android:fontFamily="@font/cabin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"/>

            <Button
                android:id="@+id/btnAccept"
                android:background="@drawable/custom_btn_background"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/cabin"
                android:textAllCaps="false"
                android:text="@string/accept" />
        </LinearLayout>
    </androidx.cardview.widget.CardView>

</RelativeLayout>